#!/bin/sh
pool=$1
shift
<%=buildapi_python%> <%=buildapi_dir%>/scripts/wailwaittimes.py -S smtp.mozilla.org -f nobody@build.mozilla.org -p $pool "$@" -W http://localhost/buildapi/reports/waittimes -e $(date -d "$(date +%Y-%m-%d)" +%s) -t 10 -z 10
